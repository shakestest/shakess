#include "pch.h"
#include <iostream>
#include <math.h>
int main()
{
	double a, b, c, P;

	//Введення початкових даних
	printf("input A: ");
	scanf_s("%lf", &a);
	printf("input B: ");
	scanf_s("%lf", &b);
	printf("input C: ");
	scanf_s("%lf", &c);
	printf("input N: ");
	P = a + b / с;
	b = a * a;
	//Виведення результату
	printf("P= %lf\n", P);
	system("pause");
	return 0;
}